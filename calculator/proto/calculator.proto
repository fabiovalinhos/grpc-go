syntax = "proto3";

package calculator;

option go_package =  "github.com/fabiovalinhos/grpc-go/calculator/proto"; 

import "sum.proto"; 

service CalculatorService{
    rpc Sum(SumRequest) returns (SumResponse);
}